package main.oop.abstraction;


// to achieve 100% abstraction we used interfaces as we
public interface IAnimal {

    // the class varaibles are static final
    // so its kind of redundant to achieve overiding values that difference between abstract also we don't need keywords
    //int name = 1;


    // so interfaces is public abstract by default
    void makeNoises();
}
